# Auth Frontend Environment Variables

# API Base URL - leave empty to use relative paths (for same-origin)
# Set to full URL when running frontend separately from backend
VITE_API_BASE_URL=

# Example: If your auth backend runs on port 8000
# VITE_API_BASE_URL=http://localhost:8000

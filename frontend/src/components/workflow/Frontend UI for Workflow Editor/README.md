
  # Frontend UI for Workflow Editor

  This is a code bundle for Frontend UI for Workflow Editor. The original project is available at https://www.figma.com/design/o5TYpXgGogjbklVpZyKoeq/Frontend-UI-for-Workflow-Editor.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  